#include <stdio.h>
#include <string.h>
/**
* _strlen - Computes the length of a string.
* @s: Pointer to the string.
*
* Return: The length of the string.
*/
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
